id: sensor-app

alerts:
  SensorsTripped:
    if: 'sensor_temperature_celsius > ignoring(point) group_right  sensor_trip_point_celsius'
    for: 5m
    labels:
      severity: page
      team: infra
    alert:
      description: "Sensor in {{ $labels.zone }} has reached {{ $value }}"
